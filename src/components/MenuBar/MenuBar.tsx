import React from 'react';

const MenuBar: React.FC = () => {
  return (
    <nav className="w-full h-16 bg-green-100 border-2 border-green-300 border-dashed flex items-center justify-center">
      <h2 className="text-green-600 text-lg font-semibold">MenuBar Component</h2>
    </nav>
  );
};

export default MenuBar;
